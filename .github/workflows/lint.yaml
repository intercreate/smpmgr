name: Lint
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
        typer-version: ["0.24.0", "0.27.2"]

    steps:
      - uses: actions/checkout@v4

      - run: pipx install poetry

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - run: poetry install

      - name: Select Typer compatibility endpoint
        run: poetry run pip install "typer==${{ matrix.typer-version }}"

      - name: lint
        run: |
          shopt -s expand_aliases
          . ./envr.ps1
          lint
